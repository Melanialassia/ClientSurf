import React from "react";
import { login } from "../utils/constants";

const LoginButton = () => {
  return (
    <div>
      <button>{login}</button>
    </div>
  );
};

export default LoginButton;
